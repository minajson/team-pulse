# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# builds
/.next/
/out/
/build

# testing
/coverage
/playwright-report
/test-results
/.playwright-mcp

# secrets — never commit a database URL or any local override
.env
.env.local
.env.*.local
.env*
!.env.example

# Team Pulse local session snapshots (memory driver). Contains live session
# data; belongs on the machine that ran the session, not in the repository.
/.data/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# hosting
.vercel

# macOS
.DS_Store
*.pem
